# Impeccable: Layout & Delight Pass

Ho analizzato il progetto seguendo le direttive di *Impeccable* per migliorare la struttura spaziale (**Layout**) e aggiungere micro-interazioni memorabili (**Delight**).

Ecco il piano d'azione:

## Open Questions
> [!IMPORTANT]
> Prima di procedere con l'esecuzione, ho bisogno di alcune tue preferenze:
> 1. **Colori non documentati**: Il detector ha trovato `rgba(0,0,0,0.06)` (sfondo chips) e `rgba(0,0,0,0.3)` nel codice. Vuoi che li aggiungiamo al `DESIGN.md` o preferisci che usi un token esistente (es. `--ink-muted` in trasparenza)?
> 2. **Border Radius**: Ci sono valori come `2px`, `10px`, `16px`, `24px` e `40px` usati nei CSS che non fanno parte della scala ufficiale. Uniformo tutto alla scala esistente o aggiungiamo questi step al design system?
> 3. **Delight**: Ti piacciono le idee proposte qui sotto per le animazioni (pendolo punchline, reazioni extra del cane)? C'è qualcosa che non ti convince?

## Proposed Changes

### 1. Layout (Spaziatura, Ritmo e Design System)
Attualmente alcune sezioni usano padding molto elevati e rigidi (es. `224px`). Li renderemo più armoniosi e aderenti al design system, pulendo i token non validi.

#### [MODIFY] `src/components/Works.css` & `Workflow.css` & `InteractiveWidgets.css`
- **Normalizzazione Padding**: Useremo un sistema di spaziature basato su scale fluide o variabili fisse più coerenti tra una sezione e l'altra, per creare un ritmo (tension/release) migliore.
- **Risoluzione Anomalie**: Sostituiremo i `border-radius` (es. `24px`, `40px`) e i colori hardcoded (es. `#fff`, `rgba(0,0,0,0.06)`) con i token CSS del progetto (o li definiremo ufficialmente nel root theme).

### 2. Delight (Micro-interazioni e Personalità)
Il portfolio ha già una forte componente fisica e giocosa. Aggiungeremo momenti di sorpresa ("Easter Eggs") che premiano l'interazione senza bloccare l'usabilità.

#### [MODIFY] `src/components/Hero.jsx` & `Hero.css`
- **Fisica della Punchline**: Al momento la puntina si può trascinare. Aggiungeremo un effetto "pendolo" (spring animation in Framer Motion): se la trascini e la rilasci bruscamente, oscillerà un po' prima di fermarsi, come un foglietto appeso a un muro di sughero.

#### [MODIFY] `src/components/InteractiveWidgets.jsx`
- **Anticipazione del Cane**: Il cane reagirà non solo se droppi il croccantino, ma anche se *lo muovi velocemente a mezz'aria* (ad esempio facendogli scodinzolare la coda più in fretta quando "vede" il croccantino muoversi, cambiando la velocità dell'animazione in CSS).

#### [MODIFY] `src/components/About.jsx`
- **Snap della Polaroid**: Aggiungeremo un rimbalzo elastico alla rotazione 3D della polaroid per farle avere un peso fisico, rendendo il "flip" visivamente più soddisfacente.

## Verification Plan

### Manual Verification
- Navigherò l'app e simulerò hover/drag per assicurarmi che le animazioni *Delight* si attivino in modo fluido e non rallentino la pagina (`prefers-reduced-motion` sarà rispettato).
- Controllerò a varie risoluzioni che le distanze tra le sezioni (`Works`, `Workflow`, `InteractiveWidgets`) scorrano in modo ritmico e proporzionato.
